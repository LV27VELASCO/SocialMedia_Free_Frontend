---
// tu Public Key
---

<!-- Animated Background Elements -->
<div class="fixed inset-0 overflow-hidden pointer-events-none">
    <div
        class="absolute top-20 left-10 w-64 h-64 bg-pink-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-element"
    >
    </div>
    <div
        class="absolute top-40 right-10 w-80 h-80 bg-purple-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-element"
        style="animation-delay: 2s;"
    >
    </div>
    <div
        class="absolute bottom-20 left-1/2 w-72 h-72 bg-indigo-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-element"
        style="animation-delay: 4s;"
    >
    </div>
</div>

<!-- Main Form Container -->
<div class="relative z-10 w-full max-w-lg mx-auto animate-fade-in">
    <!-- Logo/Brand -->
    <div class="text-center mb-8">
        <a href="/" class="text-3xl font-bold text-white mb-2">
            🚀 <span class="gradient-text2">WeeklySocial</span>
        </a>
    </div>

    <!-- Form Card -->
    <div
        class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 shadow-[0_0_30px_rgba(236,72,153,0.3)] border border-white/20 animate-slide-up"
    >
        <!-- Progress Indicator -->
        <div class="flex justify-center mb-6">
            <div class="flex items-center space-x-2">
                <div
                    class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white text-sm font-bold"
                >
                    ✓
                </div>
                <div
                    class="w-12 h-1 bg-gradient-to-r from-pink-500 to-purple-500 rounded-full"
                >
                </div>
                <div
                    class="w-8 h-8 bg-gradient-to-r from-pink-500 to-purple-500 rounded-full flex items-center justify-center text-white text-sm font-bold animate-pulse"
                >
                    2
                </div>
                <div
                    class="w-12 h-1 bg-gradient-to-r from-pink-500 to-purple-500 rounded-full"
                >
                </div>
                <div
                    class="w-8 h-8 bg-white/30 rounded-full flex items-center justify-center text-white text-sm font-bold animate-pulse"
                >
                    3
                </div>
            </div>
        </div>

        <!-- Security Badge -->
        <div class="flex justify-center mb-6">
            <div
                class="animate-pulse inline-flex items-center px-4 py-2 bg-green-500/20 border border-green-400/50 rounded-full text-green-300 text-sm font-semibold"
            >
                🔒 Conexión 100% Segura SSL
            </div>
        </div>

        <!-- Header -->
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-white mb-3">
                Validación de seguridad
            </h1>
            <p class="text-lg text-gray-200 leading-relaxed">
                Solo para verificar que eres una persona real y evitar bots
            </p>
        </div>

        <!-- Card Preview -->
        <div
            class="bg-gradient-to-br from-indigo-400 to-purple-600 rounded-xl p-5 text-white font-mono mb-6 animate-card-flip"
        >
            <div class="flex justify-between items-start mb-4">
                <div class="text-sm opacity-80">TARJETA DE CRÉDITO</div>
                <div class="text-2xl">💳</div>
            </div>
            <div class="text-xl font-mono mb-4 tracking-wider" id="cardNumber">
                •••• •••• •••• ••••
            </div>
            <div class="flex justify-between items-end">
                <div>
                    <div class="text-xs opacity-80">TITULAR</div>
                    <div class="font-semibold" id="cardName">TU NOMBRE</div>
                </div>
                <div>
                    <div class="text-xs opacity-80">VÁLIDA HASTA</div>
                    <div class="font-semibold" id="cardExpiry">MM/AA</div>
                </div>
            </div>
        </div>

        <!-- Form id="cardForm" -->
        <form class="space-y-6" id="payment-form">
            <!-- Card Number -->
            <div class="space-y-2">
                <label
                    for="cardNumber"
                    class="block text-white font-medium text-lg"
                >
                    Número de tarjeta
                </label>
                <div class="relative">
                    <div
                        id="card-number"
                        class="transition-all duration-300 focus:-translate-y-0.5 focus:shadow-[0_10px_25px_rgba(139,92,246,0.2)] w-full px-6 py-5 bg-white/40 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                    >
                    </div>
                    <div
                        class="absolute inset-y-0 right-0 flex items-center pr-6"
                    >
                        <span class="text-pink-300 text-xl" id="cardIcon"
                            >💳</span
                        >
                    </div>
                </div>
            </div>

            <!-- Card Name -->
            <div class="space-y-2">
                <label
                    for="cardName"
                    class="block text-white font-medium text-lg"
                >
                    Nombre del titular
                </label>
                <div class="relative">
                    <input
                        type="text"
                        id="cardNameInput"
                        name="cardName"
                        required
                        autocomplete="off"
                        placeholder="Como aparece en tu tarjeta"
                        class="transition-all duration-300 focus:-translate-y-0.5 focus:shadow-[0_10px_25px_rgba(139,92,246,0.2)] w-full px-6 py-3 bg-white/40 border border-white/30 rounded-2xl text-gray-700 placeholder-gray-700/30 text-base focus:outline-none focus:border-pink-400 focus:bg-white/30 font-sans"
                    />
                    <div
                        class="absolute inset-y-0 right-0 flex items-center pr-6"
                    >
                        <span class="text-pink-300 text-xl">👤</span>
                    </div>
                </div>
            </div>

            <!-- Expiry and CVV -->
            <div class="grid grid-cols-2 gap-4">
                <div class="space-y-2">
                    <label
                        for="card-expiry"
                        class="block text-white font-medium text-lg"
                    >
                        Fecha
                    </label>
                    <!-- <input
                        type="text"
                        id="expiryInput"
                        name="expiry"
                        required
                        autocomplete="off"
                        placeholder="MM/AA"
                        maxlength="5"
                        class="transition-all duration-300 focus:-translate-y-0.5 focus:shadow-[0_10px_25px_rgba(139,92,246,0.2)] w-full px-4 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                    /> -->
                    <div
                        id="card-expiry"
                        class="transition-all duration-300 focus:-translate-y-0.5 focus:shadow-[0_10px_25px_rgba(139,92,246,0.2)] w-full px-4 py-5 bg-white/40 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                    >
                    </div>
                </div>
                <div class="space-y-2">
                    <label
                        for="#card-cvc"
                        class="block text-white font-medium text-lg"
                    >
                        CVV
                    </label>
                    <!-- <input
                        type="text"
                        id="cvvInput"
                        name="cvv"
                        required
                        autocomplete="off"
                        placeholder="123"
                        maxlength="4"
                        class="transition-all duration-300 focus:-translate-y-0.5 focus:shadow-[0_10px_25px_rgba(139,92,246,0.2)] w-full px-4 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                    /> -->
                    <div
                        id="card-cvc"
                        class="transition-all duration-300 focus:-translate-y-0.5 focus:shadow-[0_10px_25px_rgba(139,92,246,0.2)] w-full px-4 py-5 bg-white/40 border border-white/30 rounded-2xl text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                    >
                    </div>
                </div>
            </div>

            <!-- Security Messages -->
            <div class="space-y-3">
                <div
                    class="bg-blue-500/20 border border-blue-400/30 rounded-xl p-4"
                >
                    <div class="flex items-start space-x-3">
                        <span class="text-blue-300 text-xl flex-shrink-0"
                            >🛡️</span
                        >
                        <div>
                            <p class="text-blue-100 text-sm leading-relaxed">
                                <strong>¿Por qué necesitamos tu tarjeta?</strong
                                ><br />
                                Solo para verificar que eres una persona real y evitar
                                que los bots abusen del sistema.
                            </p>
                        </div>
                    </div>
                </div>

                <div
                    class="bg-green-500/20 border border-green-400/30 rounded-xl p-4"
                >
                    <div class="flex items-start space-x-3">
                        <span class="text-green-300 text-xl flex-shrink-0"
                            >✅</span
                        >
                        <div>
                            <p class="text-green-100 text-sm leading-relaxed">
                                <strong>Garantía de seguridad:</strong><br />
                                • No realizaremos ningún cargo<br />
                                • Tus datos están encriptados<br />
                                • Puedes cancelar en cualquier momento
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Error Message -->
            <p id="message2" class="text-center text-sm mt-2"></p>

            <!-- Submit Button -->
            <button
                type="submit"
                class="transition-all duration-300 hover:-translate-y-0.5 hover:shadow-[0_15px_35px_rgba(236,72,153,0.4)] bg-pink-500 w-full bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-500 text-white font-bold text-xl py-5 px-8 rounded-2xl focus:outline-none focus:ring-4 focus:ring-pink-300/50 animate-pulse-slow"
            >
                <span class="flex items-center justify-center space-x-3">
                    <span>🚀 Validar y obtener seguidores</span>
                </span>
            </button>

            <!-- Final Security Message -->
            <div
                class="bg-yellow-500/20 border border-yellow-400/30 rounded-xl p-4 mt-4"
            >
                <div class="flex items-start space-x-3">
                    <span class="text-yellow-300 text-lg flex-shrink-0">⚡</span
                    >
                    <p class="text-yellow-100 text-sm leading-relaxed">
                        <strong>Proceso instantáneo:</strong> Una vez validada tu
                        tarjeta, recibirás tus seguidores en menos de 5 minutos.
                    </p>
                </div>
            </div>
        </form>

        <!-- Trust Indicators -->
        <div class="mt-8 pt-6 border-t border-white/20">
            <div class="grid grid-cols-2 gap-4 text-center">
                <div class="flex flex-col items-center space-y-2">
                    <span class="text-3xl">🔒</span>
                    <span class="text-gray-300 text-sm">Encriptación SSL</span>
                </div>
                <div class="flex flex-col items-center space-y-2">
                    <span class="text-3xl">🛡️</span>
                    <span class="text-gray-300 text-sm">Datos Protegidos</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Bottom Trust Message -->
    <div class="text-center mt-6">
        <p class="text-gray-300 text-sm">
            🔥 <strong>Miles de usuarios</strong> han validado su cuenta de forma
            segura
        </p>
    </div>
</div>
<!-- Cargar Stripe desde su CDN -->
<script src="https://js.stripe.com/v3/"></script>
<script>
    window.addEventListener('load', () => {
        const stripe =  Stripe(import.meta.env.PUBLIC_STRIPE_PK);
        const appearance = {
            theme: "stripe",

            variables: {
                colorPrimary: "#0570de",
                colorBackground: "#ffffff",
                colorText: "#30313d",
                colorDanger: "#df1b41",
                fontFamily: "Ideal Sans, system-ui, sans-serif",
                spacingUnit: "2px",
                borderRadius: "4px",
                // See all possible variables below
            },
        };
        const elements = stripe.elements();

        const cardNumber = elements.create("cardNumber");
        cardNumber.mount("#card-number");

        const cardExpiry = elements.create("cardExpiry");
        cardExpiry.mount("#card-expiry");

        const cardCvc = elements.create("cardCvc");
        cardCvc.mount("#card-cvc");

        const form = document.getElementById("payment-form");

        if (!form) return; // Evita el error de null

        form.addEventListener("submit", async (e) => {
            e.preventDefault();

            // Show loading state
            const button = document.querySelector<HTMLButtonElement>('button[type="submit"]');
            const messageEl = document.getElementById("message2");
            if (!button ||!messageEl) return;
            const originalText = button.innerHTML;
            button.innerHTML =
                '<span class="flex items-center justify-center space-x-2"><span class="animate-spin">⏳</span><span>Validando tarjeta...</span></span>';
            button.disabled = true;
            

            const { paymentMethod, error } = await stripe.createPaymentMethod({
                type: "card",
                card: cardNumber,
            });

            if (error) {
                messageEl.textContent = error.message;
                messageEl.className =
                    "text-red-300 text-center text-sm bg-red-500/20 p-4 rounded-xl border border-red-400/30";
                button.innerHTML = originalText;
                button.disabled = false;
                return;
            }

            const res = await validate_pay(paymentMethod, originalText);
        });
    })

    async function getToken() {
        const res = await fetch(`${import.meta.env.PUBLIC_API}/token`);

        if (!res.ok) {
            throw new Error("Error al obtener el token");
        }

        const data = await res.json();
        return data.token; // O la propiedad que te envíe el token
    }

    async function validate_pay(paymentMethod:any, originalText:any) {
        const token = await getToken();

        const dataRegister = JSON.parse(localStorage.getItem("Register") as string);
        const button = document.querySelector<HTMLButtonElement>('button[type="submit"]');
        const messageEl = document.getElementById("message2");
        if (!button ||!messageEl) return;

        const res = await fetch(
            `${import.meta.env.PUBLIC_API}/validate-pay-method`,
            {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                    Authorization: `Bearer ${token}`,
                },
                body: JSON.stringify({
                    paymentMethodId: paymentMethod.id,
                    name: dataRegister.nombre,
                    email: dataRegister.email,
                    socialMedia: dataRegister.redSocial,
                    url: dataRegister.url,
                    accion: dataRegister.accion,
                }),
            },
        );
        const data = await res.json();
        if (!res.ok) {
            messageEl.textContent = data.detail;
            messageEl.className =
                "text-red-300 text-center text-sm bg-red-500/20 p-4 rounded-xl border border-red-400/30";
            button.innerHTML = originalText;
            button.disabled = false;
            return;
        }

        if (data.error) {
            messageEl.textContent = data.error;
            messageEl.className =
                "text-red-300 text-center text-sm bg-red-500/20 p-4 rounded-xl border border-red-400/30";
            button.innerHTML = originalText;
            button.disabled = false;
            return;
        } else {
            // Simulate form processing
            setTimeout(() => {
                window.location.href = "confirmation-success";

                // Reset button
                button.innerHTML = originalText;
                button.disabled = false;
            }, 2000);
            messageEl.textContent = data.message;
            messageEl.className = "text-green-600 text-center text-sm";
            button.innerHTML = originalText;
            button.disabled = false;
        }
    }
</script>
<!-- JavaScript for Form Enhancement -->
<script is:inline>
    const cardNameInput = document.getElementById("cardNameInput");

    // Update card preview
    function updateCardPreview() {
        const cardName = document.getElementById("cardName");

        // Update card name
        cardName.textContent = cardNameInput.value.toUpperCase() || "TU NOMBRE";
    }

    // Update card name
    cardNameInput.addEventListener("input", updateCardPreview);

    // Input animations
    const inputs = document.querySelectorAll("input");
    inputs.forEach((input) => {
        input.addEventListener("focus", function () {
            this.parentElement.style.transform = "scale(1.02)";
        });

        input.addEventListener("blur", function () {
            this.parentElement.style.transform = "scale(1)";
        });
    });

    // Add floating animation to form on load
    window.addEventListener("load", function () {
        isRegistered = localStorage.getItem("Register");
        if (!isRegistered) {
            this.location.href = "register";
        }
    });
</script>
