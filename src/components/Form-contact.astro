<!-- Animated Background Elements -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-10 w-64 h-64 bg-pink-500 rounded-full mix-blend-multiply filter blur-xl opacity-10 floating-element"></div>
        <div class="absolute top-40 right-10 w-80 h-80 bg-purple-500 rounded-full mix-blend-multiply filter blur-xl opacity-10 floating-element" style="animation-delay: 2s;"></div>
        <div class="absolute bottom-20 left-1/2 w-72 h-72 bg-indigo-500 rounded-full mix-blend-multiply filter blur-xl opacity-10 floating-element" style="animation-delay: 4s;"></div>
    </div>

    <!-- Header -->
    <header class="relative z-50 py-6">
        <div class="container mx-auto px-6">
            <div class="flex items-center justify-between">
                <!-- Logo -->
                <div class="text-2xl font-bold text-white">
                    🚀 <span class="gradient-text2">WklySocial</span>
                </div>
                
                <!-- Back Button -->
                <button onclick="history.back()" class="back-button  flex items-center space-x-2 px-6 py-3 bg-white/10 backdrop-blur-lg rounded-full text-white/70 border border-white/20">
                    <span>←</span>
                    <span>Volver</span>
                </button>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="relative z-10 container mx-auto px-6 pb-12">
        
        <!-- Page Title -->
        <div class="text-center mb-12 animate-fade-in">
            <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
                Contacto y <span class="gradient-text2">Soporte</span>
            </h1>
            <p class="text-xl text-gray-300">
                Estamos aquí para ayudarte. <strong>Respuesta garantizada en menos de 24 horas</strong>
            </p>
        </div>

        <!-- Contact Methods Grid -->
        <div class="flex gap-8 mb-16 animate-slide-up justify-center">
            
            <!-- Email Support -->
            <div class="card-hover max-w-4xl  bg-white/10 backdrop-blur-lg rounded-3xl p-8 text-center border border-white/20">
                <div class="text-6xl mb-4 animate-pulse-slow">📧</div>
                <h3 class="text-2xl font-bold text-white mb-4">Email Soporte</h3>
                <p class="text-gray-300 mb-6">Para consultas generales y soporte técnico</p>
                <a href="mailto:soporte@wklysocial.com" class="inline-block px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-500 text-white font-semibold rounded-full hover:scale-105 transform transition-all duration-300">
                    soporte@wklysocial.com
                </a>
                <div class="mt-4 text-sm text-gray-400">
                    ⏰ Respuesta: 2-6 horas
                </div>
            </div>
        </div>

        <!-- Contact Form Section -->
        <div class="max-w-4xl mx-auto mb-16 hidden">
            <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 md:p-12 content-glow border border-white/20 animate-slide-up">
                
                <div class="text-center mb-8">
                    <h2 class="text-3xl font-bold text-white mb-4">
                        Envíanos un mensaje
                    </h2>
                    <p class="text-lg text-gray-300">
                        Completa el formulario y te responderemos lo antes posible
                    </p>
                </div>

                <!-- Contact Form -->
                <form class="space-y-6" id="contactForm">
                    
                    <div class="grid md:grid-cols-2 gap-6">
                        <!-- Name -->
                        <div class="space-y-2">
                            <label for="nombre" class="block text-white font-medium text-lg">
                                Nombre completo
                            </label>
                            <div class="relative">
                                <input 
                                    type="text" 
                                    id="nombre" 
                                    name="nombre" 
                                    required
                                    placeholder="Tu nombre completo"
                                    autocomplete="off"
                                    class="input-focus w-full px-6 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                                >
                                <div class="absolute inset-y-0 right-0 flex items-center pr-6">
                                    <span class="text-pink-300 text-xl">👤</span>
                                </div>
                            </div>
                        </div>

                        <!-- Email -->
                        <div class="space-y-2">
                            <label for="email" class="block text-white font-medium text-lg">
                                Correo electrónico
                            </label>
                            <div class="relative">
                                <input 
                                    type="email" 
                                    id="email" 
                                    name="email" 
                                    required
                                    autocomplete="off"
                                    placeholder="tu@email.com"
                                    class="input-focus w-full px-6 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                                >
                                <div class="absolute inset-y-0 right-0 flex items-center pr-6">
                                    <span class="text-pink-300 text-xl">📧</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Subject -->
                    <div class="space-y-2">
                        <label for="asunto" class="block text-white font-medium text-lg">
                            Tipo de consulta
                        </label>
                        <select 
                            id="asunto" 
                            name="asunto" 
                            required
                            autocomplete="off"
                            class="input-focus w-full px-6 py-4 bg-white/20 border border-white/30 rounded-2xl text-white text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30"
                        >
                            <option value="" class="bg-gray-800">Selecciona el tipo de consulta</option>
                            <option value="soporte-tecnico" class="bg-gray-800">🔧 Soporte Técnico</option>
                            <option value="problemas-pago" class="bg-gray-800">💳 Problemas de Pago</option>
                            <option value="cancelacion" class="bg-gray-800">❌ Cancelación de Suscripción</option>
                            <option value="reembolso" class="bg-gray-800">💰 Solicitud de Reembolso</option>
                            <option value="cuenta-suspendida" class="bg-gray-800">🚫 Cuenta Suspendida</option>
                            <option value="sugerencias" class="bg-gray-800">💡 Sugerencias</option>
                            <option value="otro" class="bg-gray-800">❓ Otro</option>
                        </select>
                    </div>

                    <!-- Message -->
                    <div class="space-y-2">
                        <label for="mensaje" class="block text-white font-medium text-lg">
                            Mensaje
                        </label>
                        <textarea 
                            id="mensaje" 
                            name="mensaje" 
                            required
                            rows="6"
                            autocomplete="off"
                            placeholder="Describe tu consulta con el mayor detalle posible..."
                            class="input-focus w-full px-6 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-gray-300 text-lg focus:outline-none focus:border-pink-400 focus:bg-white/30 resize-none"
                        ></textarea>
                    </div>

                    <!-- Priority -->
                    <div class="bg-yellow-500/20 border border-yellow-400/30 rounded-xl p-4">
                        <div class="flex items-center space-x-3">
                            <input type="checkbox" id="urgente" name="urgente" autocomplete="off" class="w-5 h-5 text-pink-500">
                            <label for="urgente" class="text-yellow-100 font-medium">
                                ⚡ Marcar como urgente (problemas de pago o cuenta suspendida)
                            </label>
                        </div>
                    </div>

                    <!-- Submit Button -->
                    <button 
                        type="submit" 
                        class="button-hover  w-full bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-500 text-white font-bold text-xl py-5 px-8 rounded-2xl focus:outline-none focus:ring-4 focus:ring-pink-300/50"
                    >
                        <span class="flex items-center justify-center space-x-3">
                            <span>📤 Enviar Mensaje</span>
                        </span>
                    </button>

                    <!-- Response Time Info -->
                    <div class="bg-green-500/20 border border-green-400/30 rounded-xl p-4 text-center">
                        <p class="text-green-100">
                            <strong>⏰ Tiempo de respuesta garantizado:</strong><br>
                            Consultas normales: 6-24 horas | Urgentes: 1-3 horas
                        </p>
                    </div>

                </form>

            </div>
        </div>

        <!-- FAQ Section -->
        <div class="max-w-4xl mx-auto mb-16">
            <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 md:p-12 content-glow border border-white/20">
                
                <div class="text-center mb-8">
                    <h2 class="text-3xl font-bold text-white mb-4">
                        Preguntas Frecuentes
                    </h2>
                    <p class="text-lg text-gray-300">
                        Encuentra respuestas rápidas a las consultas más comunes
                    </p>
                </div>

                <div class="space-y-4">
                    
                    <!-- FAQ Item 1 -->
                    <div class="bg-white/5 rounded-2xl border border-white/10 overflow-hidden">
                        <button class="faq-toggle w-full px-6 py-4 text-left flex items-center justify-between text-white hover:bg-white/5 transition-colors">
                            <span class="font-semibold">❓ ¿Cómo cancelo mi suscripción?</span>
                            <span class="text-2xl">+</span>
                        </button>
                        <div class="faq-content hidden px-6 pb-4 text-gray-300">
                            <p>Puedes cancelar tu suscripción en cualquier momento desde tu panel de usuario o enviándonos un email a soporte@wklysocial.com. La cancelación será efectiva al final del período de facturación actual.</p>
                        </div>
                    </div>

                    <!-- FAQ Item 2 -->
                    <div class="bg-white/5 rounded-2xl border border-white/10 overflow-hidden">
                        <button class="faq-toggle w-full px-6 py-4 text-left flex items-center justify-between text-white hover:bg-white/5 transition-colors">
                            <span class="font-semibold">💳 ¿Cuándo se reembolsa el euro de validación?</span>
                            <span class="text-2xl">+</span>
                        </button>
                        <div class="faq-content hidden px-6 pb-4 text-gray-300">
                            <p>El euro de validación se reembolsa automáticamente en un plazo de 3-5 días hábiles después del registro, dependiendo de las políticas de tu banco.</p>
                        </div>
                    </div>

                    <!-- FAQ Item 3 -->
                    <div class="bg-white/5 rounded-2xl border border-white/10 overflow-hidden">
                        <button class="faq-toggle w-full px-6 py-4 text-left flex items-center justify-between text-white hover:bg-white/5 transition-colors">
                            <span class="font-semibold">⏰ ¿Cuánto tardan en llegar los seguidores?</span>
                            <span class="text-2xl">+</span>
                        </button>
                        <div class="faq-content hidden px-6 pb-4 text-gray-300">
                            <p>Los seguidores comienzan a llegar en un plazo de 5-15 minutos después de completar el registro. El proceso completo puede tomar hasta 24 horas dependiendo de la cantidad solicitada.</p>
                        </div>
                    </div>

                    <!-- FAQ Item 4 -->
                    <div class="bg-white/5 rounded-2xl border border-white/10 overflow-hidden">
                        <button class="faq-toggle w-full px-6 py-4 text-left flex items-center justify-between text-white hover:bg-white/5 transition-colors">
                            <span class="font-semibold">🔒 ¿Es seguro proporcionar mis datos de tarjeta?</span>
                            <span class="text-2xl">+</span>
                        </button>
                        <div class="faq-content hidden px-6 pb-4 text-gray-300">
                            <p>Sí, es completamente seguro. Utilizamos Stripe, uno de los procesadores de pago más seguros del mundo, con encriptación SSL y cumplimiento PCI DSS. No almacenamos directamente los datos de tu tarjeta.</p>
                        </div>
                    </div>

                    <!-- FAQ Item 5 -->
                    <div class="bg-white/5 rounded-2xl border border-white/10 overflow-hidden">
                        <button class="faq-toggle w-full px-6 py-4 text-left flex items-center justify-between text-white hover:bg-white/5 transition-colors">
                            <span class="font-semibold">📱 ¿Funciona con todas las redes sociales?</span>
                            <span class="text-2xl">+</span>
                        </button>
                        <div class="faq-content hidden px-6 pb-4 text-gray-300">
                            <p>Actualmente ofrecemos servicios para Instagram, TikTok, Twitter y YouTube. Estamos trabajando para añadir más plataformas próximamente.</p>
                        </div>
                    </div>

                </div>

            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="relative z-10 py-8 text-center">
        <div class="container mx-auto px-6">
            <p class="text-gray-400 text-sm">
                © 2024 WklySocial. Estamos aquí para ayudarte 24/7
            </p>
        </div>
    </footer>


    <!-- JavaScript -->
    <script is:inline>
        // Contact form submission
        document.getElementById('contactForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Get form data
            const formData = new FormData(this);
            const data = Object.fromEntries(formData);
            
            // Show loading state
            const button = this.querySelector('button[type="submit"]');
            const originalText = button.innerHTML;
            button.innerHTML = '<span class="flex items-center justify-center space-x-2"><span class="animate-spin">⏳</span><span>Enviando...</span></span>';
            button.disabled = true;
            
            // Simulate form submission
            setTimeout(() => {
                alert(`¡Gracias ${data.nombre}! Tu mensaje ha sido enviado. Te responderemos en ${data.urgente ? '1-3 horas' : '6-24 horas'}.`);
                
                // Reset form
                this.reset();
                
                // Reset button
                button.innerHTML = originalText;
                button.disabled = false;
            }, 2000);
        });

        // FAQ Toggle functionality
        document.querySelectorAll('.faq-toggle').forEach(button => {
            button.addEventListener('click', function() {
                const content = this.nextElementSibling;
                const icon = this.querySelector('span:last-child');
                
                if (content.classList.contains('hidden')) {
                    content.classList.remove('hidden');
                    icon.textContent = '−';
                    icon.style.transform = 'rotate(180deg)';
                } else {
                    content.classList.add('hidden');
                    icon.textContent = '+';
                    icon.style.transform = 'rotate(0deg)';
                }
            });
        });

        // Input animations
        const inputs = document.querySelectorAll('input, textarea, select');
        inputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transform = 'scale(1.02)';
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'scale(1)';
            });
        });

        // Add entrance animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe elements for animation
        document.querySelectorAll('.animate-slide-up').forEach(element => {
            element.style.opacity = '0';
            element.style.transform = 'translateY(30px)';
            element.style.transition = 'all 0.6s ease-out';
            observer.observe(element);
        });
    </script>