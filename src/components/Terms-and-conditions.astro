---
import { useI18n } from '../i18n';
const { translate, currentLang } = useI18n(Astro);
const prohibiteds = translate('terms.usage.prohibited.items') as unknown as {}[]
const services = translate('terms.service.items') as unknown as {}[]
const access = translate('terms.service.access.items') as unknown as {}[]
const liabilitys = translate('terms.liability.items') as unknown as {}[]
const generals = translate('terms.general.items') as unknown as {}[]
---
<!-- Main Content -->
<section class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <!-- Page Title -->
        <div class="text-center mb-12 animate-fade-in">
            <h1 class="text-4xl md:text-5xl font-bold text-white mb-4 ">
                {translate('terms.title')} <span class="gradient-text">{translate('terms.title.span')}</span>
            </h1>
            <p class="text-xl text-gray-300">
                {translate('terms.date')}
            </p>
        </div>

        <!-- Content Card -->
        <div class="bg-white/5 backdrop-blur-lg rounded-2xl border border-white/10 p-8 md:p-12">
            <!-- Content -->
            <div class="space-y-8 text-gray-300 leading-relaxed">
                <!-- Section 1 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">1. {translate('terms.introduction.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>1.1 </strong>{translate('terms.introduction.text1')}</p>
                        <p><strong>1.2 </strong>{translate('terms.introduction.text2')}</p>
                    </div>
                </section>

                <!-- Section 2 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">2. {translate('terms.definitions.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>2.1 </strong>{translate('terms.definitions.text1')}</p>
                        <p><strong>2.2 </strong>{translate('terms.definitions.text2')}</p>
                        <p><strong>2.3 </strong>{translate('terms.definitions.text3')}</p>
                    </div>
                </section>

                <!-- Section 3 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">3. {translate('terms.registration.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>3.1 </strong>{translate('terms.registration.text1')}</p>
                        <p><strong>3.2 </strong>{translate('terms.registration.text2')}</p>
                        <p><strong>3.3 </strong>{translate('terms.registration.text3')}</p>
                    </div>
                </section>

                <!-- Section 4 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">4. {translate('terms.usage.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>4.1 </strong>{translate('terms.usage.text1')}</p>
                        <p><strong>4.2 </strong>{translate('terms.usage.text2')}</p>
                        <p><strong>4.3 </strong>{translate('terms.usage.text3')}</p>
                        <p class="pl-6">{translate('terms.usage.prohibited.title')}</p>
                        <ul class="list-disc pl-12 space-y-2">
                            {
                                prohibiteds.map((item)=>(
                                    <li>{item}</li>
                                ))
                            }
                        </ul>
                    </div>
                </section>

                <!-- Section 5 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">5. {translate('terms.service.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>5.1 </strong>{translate('terms.service.text1')}</p>
                        <ul class="list-disc pl-12 space-y-2">
                            {
                                services.map((item)=>(
                                    <li>{item}</li>
                                ))
                            }
                            
                        </ul>
                        <p><strong>5.2 </strong>{translate('terms.service.text2')}</p>
                        <p><strong>5.3 </strong>{translate('terms.service.text3')}</p>
                        <ul class="list-disc pl-12 space-y-2">
                            {
                                access.map((item)=>(
                                    <li>{item}</li>
                                ))
                            }
                        </ul>
                        <p><strong>5.4 </strong>{translate('terms.service.text4')}</p>
                        <p><strong>5.5 </strong>{translate('terms.service.text5')}</p>
                        <p><strong>5.6 </strong>{translate('terms.service.text6')}</p>
                        <p><strong>5.7 </strong>{translate('terms.service.text7')}</p>
                    </div>
                </section>

                <!-- Section 6 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">6. {translate('terms.intellectual.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>6.1 </strong>{translate('terms.intellectual.text1')}</p>
                        <p><strong>6.2 </strong>{translate('terms.intellectual.text2')}</p>
                        <p><strong>6.3 </strong>{translate('terms.intellectual.text3')}</p>
                        <p><strong>6.4 </strong>{translate('terms.intellectual.text4')}</p>
                    </div>
                </section>

                <!-- Section 7 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">7. {translate('terms.liability.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>7.1 </strong>{translate('terms.liability.text1')}</p>
                        <p><strong>7.2 </strong>{translate('terms.liability.text2')}</p>
                        <ul class="list-disc pl-12 space-y-2">
                            {
                                liabilitys.map((item)=>(
                                <li>{item}</li>
                                ))
                            }
                        </ul>
                        <p><strong>7.3 </strong>{translate('terms.liability.text3')}</p>
                        <p><strong>7.4 </strong>{translate('terms.liability.text4')}</p>
                        <p><strong>7.5 </strong>{translate('terms.liability.text5')}</p>
                    </div>
                </section>

                <!-- Section 8 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">8. {translate('terms.privacy.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>8.1 </strong>{translate('terms.privacy.text1')} <a href={`/${currentLang}/privacity`}  class="text-blue-400 hover:text-blue-300 underline">{translate('terms.privacy.text1.span')}</a>.</p>
                    </div>
                </section>

                <!-- Section 9 -->
                <section>
                    <h2 class="text-2xl font-bold text-purple-400 mb-4">9. {translate('terms.general.title')}</h2>
                    <div class="space-y-3">
                        <p><strong>9.1 </strong>{translate('terms.general.text1')}</p>
                        <p><strong>9.2 </strong>{translate('terms.general.text2')}</p>
                        <p><strong>9.3 </strong>{translate('terms.general.text3')}</p>
                        <ul class="list-none pl-6 space-y-1">
                            <li><strong>{translate('terms.general.email.title')}</strong> <a href="mailto:contact@trendyup.es" class="text-blue-400 hover:text-blue-300">{translate('terms.general.email.text')}</a></li>
                            <li><strong>{translate('terms.general.address.title')}</strong> {translate('terms.general.address.text')}</li>
                        </ul>
                        <p><strong>9.4 </strong>{translate('terms.general.text4')}</p>
                        <ul class="list-disc pl-12 space-y-2">
                            {
                                generals.map((item)=>(
                                <li>{item}</li>
                                ))
                            }
                        </ul>
                        <p><strong>9.5 </strong>{translate('terms.general.text5')}</p>
                    </div>
                </section>

                <!-- Contact Section -->
                <section class="mt-12 pt-8 border-t border-white/10">
                    <h3 class="text-xl font-bold text-purple-400 mb-4">{translate('terms.contact.title')}</h3>
                    <p>{translate('terms.contact.text')}</p>
                    <ul class="mt-4 space-y-2">
                        <li><strong>{translate('terms.contact.email.title')}</strong> <a href="mailto:contact@trendyup.es" class="text-blue-400 hover:text-blue-300">{translate('terms.contact.email.text')}</a></li>
                        <li><strong>{translate('terms.contact.address.title')}</strong> {translate('terms.contact.address.text')}</li>
                        <li><strong>{translate('terms.contact.company.title')}</strong> {translate('terms.contact.company.text')}</li>
                    </ul>
                </section>
            </div>
        </div>
</section>

<!-- Scroll to Top Button -->
<button id="scrollToTop"
    class="fixed  bottom-8 right-8 w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-500 text-white rounded-full shadow-lg hover:scale-110 transform transition-all duration-300 opacity-0 pointer-events-none z-50">
    ↑
</button>
<!-- JavaScript -->
<script is:inline>
    // Scroll to top functionality
    const scrollToTopBtn = document.getElementById("scrollToTop");

    window.addEventListener("scroll", () => {
        if (window.pageYOffset > 300) {
            scrollToTopBtn.style.opacity = "1";
            scrollToTopBtn.style.pointerEvents = "auto";
        } else {
            scrollToTopBtn.style.opacity = "0";
            scrollToTopBtn.style.pointerEvents = "none";
        }
    });

    scrollToTopBtn.addEventListener("click", () => {
        window.scrollTo({
            top: 0,
            behavior: "smooth",
        });
    });

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
        anchor.addEventListener("click", function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute("href"));
            if (target) {
                target.scrollIntoView({
                    behavior: "smooth",
                    block: "start",
                });
            }
        });
    });

    // Add entrance animations to sections
    const observerOptions = {
        threshold: 0.1,
        rootMargin: "0px 0px -50px 0px",
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = "1";
                entry.target.style.transform = "translateY(0)";
            }
        });
    }, observerOptions);

    // Observe all sections
    document.querySelectorAll("section").forEach((section) => {
        section.style.opacity = "0";
        section.style.transform = "translateY(20px)";
        section.style.transition = "all 0.6s ease-out";
        observer.observe(section);
    });
</script>
